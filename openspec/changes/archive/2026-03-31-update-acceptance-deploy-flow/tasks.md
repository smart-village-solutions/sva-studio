## 1. Implementation

- [x] 1.1 `runtime-env` um `precheck` und orchestrierten `deploy` für `acceptance-hb` ergänzen
- [x] 1.2 Release-Klassen `app-only` und `schema-and-app` inklusive Wartungsfenster-Regel einführen
- [x] 1.3 Deploy-Reports als JSON und Markdown unter `artifacts/runtime/deployments/` schreiben
- [x] 1.4 Root-Skripte und manuellen GitHub-Workflow für den kanonischen Acceptance-Deploy ergänzen
- [x] 1.5 Swarm-Stack und Runbooks auf den neuen Deployvertrag anpassen
- [x] 1.6 Unit-Tests für CLI-Optionen und Report-Format ergänzen
- [x] 1.7 Betroffene arc42-Abschnitte aktualisieren
