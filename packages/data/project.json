{
  "name": "data",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/data/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsc -p packages/data/tsconfig.lib.json"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('lint not configured for @sva/data')\""
      }
    },
    "test:unit": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('unit tests not configured for @sva/data (coverage-exempt)')\""
      }
    },
    "test:coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('coverage tests not configured for @sva/data (coverage-exempt)')\""
      }
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('integration tests not configured for @sva/data (coverage-exempt)')\""
      }
    }
  },
  "tags": ["scope:data", "type:lib"]
}
