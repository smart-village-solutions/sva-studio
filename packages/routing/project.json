{
  "name": "routing",
  "projectType": "library",
  "sourceRoot": "packages/routing/src",
  "prefix": "",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "tsc -p packages/routing/tsconfig.lib.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @sva/routing exec vitest run"
      },
      "configurations": {
        "watch": {
          "command": "pnpm --filter @sva/routing exec vitest"
        }
      }
    }
  },
  "tags": ["type:core", "scope:routing"]
}
