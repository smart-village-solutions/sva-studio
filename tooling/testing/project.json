{
  "name": "tooling-testing",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "tooling/testing/tests",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["tooling/testing/tests/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "test:unit": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest --root tooling/testing run tests --reporter=verbose --config vitest.config.ts --passWithNoTests"
      }
    },
    "test:coverage": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest --root tooling/testing run tests --coverage --reporter=verbose --config vitest.config.ts --passWithNoTests"
      }
    },
    "test:integration": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node -e \"console.log('integration tests not configured for tooling-testing')\""
      }
    }
  },
  "tags": ["scope:tooling", "type:lib"]
}
